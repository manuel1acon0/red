logging:
  level:
    com:
      example: TRACE
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=red
    username: red
    password: password
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  jpa:
    show-sql: true
server:
  port: 8081